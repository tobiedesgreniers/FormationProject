import { FilterWithPipe } from './filter-with.pipe';

describe('FilterWithPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWithPipe();
    expect(pipe).toBeTruthy();
  });
});
